---
description: CodeGraph — use MyRoguelikeGame rule when indexing game code; this stub applies in Docs/ only
alwaysApply: false
globs: Docs/**
---

# CodeGraph（Lyra KB）

本仓库编辑 `Docs/**` 时：结构查询用 CodeGraph MCP（`codegraph_context` / `codegraph_trace` 等）。

游戏代码（`MyRoguelikeGame/Source/**`）的 CodeGraph 规则以 **MyRoguelikeGame** 仓 `.cursor/rules/codegraph.mdc` 为准（多根工作区已注入，勿重复读本文）。
